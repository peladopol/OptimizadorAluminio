<UserControl x:Class="OptimizadorAluminio.UI.Views.OptimizarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:OptimizadorAluminio.UI.Controls">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Barra de acciones -->
            <RowDefinition Height="*"/>      <!-- Canvas -->
        </Grid.RowDefinitions>

        <!-- Barra superior -->
        <Border Grid.Row="0"
                Padding="8"
                Background="#F0F0F0"
                BorderBrush="#C0C0C0"
                BorderThickness="0 0 0 1">

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Content="Imprimir"
                        Width="120"
                        Height="30"
                        Click="BtnImprimir_Click"/>
            </StackPanel>
        </Border>

        <!-- Canvas de cortes -->
        <controls:CortesCanvas x:Name="CortesCanvas"
                               Grid.Row="1"/>
    </Grid>
</UserControl>
